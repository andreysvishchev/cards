import React from 'react';

import Button from '../../components/button/Button';

const Test = () => {
  return (
    <div>
      <Button title="Кнопка" submit={false} />
    </div>
  );
};

export default Test;
